(function () {
    "use strict";

    angular.module("designer").service("DesignerService", DesignerService);

    DesignerService.$inject = ["$log", "$http"];

    function DesignerService($log, $http) {

    };
})();
